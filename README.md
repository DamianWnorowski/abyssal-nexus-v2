# Abyssal Nexus v2

Polydynamic Recursive Multi-Reality Proxy Superstructure.

Rust/Axum core (Phase 1 MVP), TypeScript frontend, Python RL sidecar, Cloud Run deployment, RTX 3080 local GPU.

## Quick Start

```bash
cargo run
```

`/api/ai-chat` endpoint live @ http://localhost:3000